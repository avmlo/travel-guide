# ===========================================
# PAYLOAD CMS CONFIGURATION (REQUIRED)
# ===========================================

# PostgreSQL Database Connection
# REQUIRED: You must provide a real PostgreSQL database URL
# For Vercel: Use Vercel Postgres, Neon, Supabase, or any PostgreSQL provider
# Format: postgresql://username:password@host:port/database
DATABASE_URL=postgresql://user:password@host:port/database

# Payload Secret Key
# REQUIRED: Generate a secure random string (32+ characters)
# Run this command: openssl rand -base64 32
PAYLOAD_SECRET=your-super-secret-key-at-least-32-characters-long

# ===========================================
# LEGACY SUPABASE (Temporary - for migration)
# ===========================================
# These are placeholders for backward compatibility
# Will be removed once all code is migrated to Payload CMS
NEXT_PUBLIC_SUPABASE_URL=https://placeholder.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=placeholder-key
SUPABASE_SERVICE_ROLE_KEY=placeholder-service-key

# ===========================================
# SERVER CONFIGURATION
# ===========================================
NEXT_PUBLIC_SERVER_URL=http://localhost:3000

# Optional: AI Integration (if using Gemini)
GEMINI_API_KEY=your-gemini-api-key
GOOGLE_CLOUD_API_KEY=your-google-cloud-api-key
