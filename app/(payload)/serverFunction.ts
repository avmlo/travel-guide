'use server'

import type { ServerFunctionClient } from 'payload'

// Server function handler for Payload admin panel
export const serverFunction: ServerFunctionClient = async (args) => {
  // This function is called by the Payload admin panel to execute server-side operations
  // The actual implementation is handled by Payload internally
  return null
}
